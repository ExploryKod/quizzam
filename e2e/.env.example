# Copier vers e2e/.env (déjà couvert par .gitignore via .env à la racine)
# Clé API Web Firebase (même que dans la config client) — requise seulement pour e2e avec AUTH_TYPE=FIREBASE
# En CI : définir le secret GitHub E2E_FIREBASE_WEB_API_KEY
E2E_FIREBASE_WEB_API_KEY=
